<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph_main"
    app:startDestination="@id/fragment_movie">

    <fragment
        android:id="@+id/fragment_movie"
        android:name="id.xxx.submission.ui.main.movie.MovieFragment"
        android:label="@string/movies"
        tools:layout="@layout/fragment_movie" />

    <fragment
        android:id="@+id/fragment_tv"
        android:name="id.xxx.submission.ui.main.tv.TvFragment"
        android:label="@string/tv_show"
        tools:layout="@layout/fragment_tv" />
</navigation>